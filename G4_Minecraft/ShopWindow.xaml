<Page x:Class="G4_WPF_PROJECT.ShopWindow"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:G4_WPF_PROJECT"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ShopWindow">

    <Grid>
        <DataGrid x:Name="prodlist"
              AutoGenerateColumns="False"
              Width="700"
              Margin="50,42,50,144"
              Background="Transparent">
            <DataGrid.Columns>
                <DataGridTextColumn Header="name" Binding="{Binding Name}" />
                <DataGridTextColumn Header="price" Binding="{Binding Price}" />
                <DataGridTextColumn Header="amount" Binding="{Binding Amount}" />
                <DataGridTemplateColumn Header="Image" Width="100" IsReadOnly="True">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Image Source="{Binding Imgsource}" Width="60" Height="60"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Button x:Name="buy" Content="사기" HorizontalAlignment="Left" Height="36" Margin="181,339,0,0" VerticalAlignment="Top" Width="179" Click="Buy_Click"/>
        <Button x:Name="sell" Content="팔기" HorizontalAlignment="Left" Height="36" Margin="443,339,0,0" VerticalAlignment="Top" Width="179"/>
        <TextBlock x:Name="money" HorizontalAlignment="Left" Margin="630,19,0,0" TextWrapping="Wrap" Text="{Binding Money}" VerticalAlignment="Top" Width="120"/>
        <Button Content="mypage" HorizontalAlignment="Center" Height="22" Margin="0,405,0,0" VerticalAlignment="Top" Width="130" Click="MyPage_Click"/>

    </Grid>
</Page>
